{
  "name": "ngraminator",
  "version": "2.0.0",
  "description": "A utility for making ngrams from Arrays",
  "main": "index.js",
  "scripts": {
    "compile-for-browser": "webpack",
    "empty-sandbox": "rm -rf test/sandbox && mkdir test/sandbox",
    "test": "npm run empty-sandbox && npm run compile-for-browser && tape ./test/test.js && cat test/sandbox/bundle.js | tape-run && rm -rf test/sandbox && standard --fix index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fergiemcdowall/ngraminator"
  },
  "author": "Fergus McDowall <fergusmcdowal@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "standard": "^13.0.2",
    "webpack": "^4.36.1",
    "webpack-cli": "^3.3.6",
    "tape": "^4.11.0",
    "tape-run": "^6.0.0"
  }
}
